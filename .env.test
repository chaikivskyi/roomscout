# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'

# DATABASE_URL must be set in .env.test.local (gitignored): Postgres runs in
# Docker on a DYNAMIC host port and .env.local is NOT loaded in the test env.
# Discover the port with: docker compose port database 5432
# The test database gets a "_test" suffix automatically (see doctrine.yaml).

CATALOG_SEARCH_MIN_MATCH_SCORE=0.5
