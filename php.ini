; Project-local PHP overrides, picked up by the Symfony CLI dev server.
; Production must set the same limits in its FPM pool config.

; Project image uploads are validated at 30 MiB (ProjectRequest).
upload_max_filesize = 30M
post_max_size = 35M
